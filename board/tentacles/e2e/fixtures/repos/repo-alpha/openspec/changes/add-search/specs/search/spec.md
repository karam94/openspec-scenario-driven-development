## ADDED Requirements

### Requirement: Full-text search across all discovered changes

The board SHALL let the user search across every discovered change.

#### Scenario: A query matches change names

- **GIVEN** several discovered changes
- **WHEN** the user types a query
- **THEN** only matching changes remain visible
